{% extends 'base.html.twig' %}

{% block title %}Theme {{ theme.name }}{% endblock %}

{% block body %}
    <h1>Theme {{ theme.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ theme.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ theme.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_theme_index') }}">retour à la liste</a>

    <a href="{{ path('admin_theme_edit', {'id': theme.id}) }}"><i class="far fa-edit"></a>

    {{ include('theme/_delete_form.html.twig') }}
{% endblock %}
