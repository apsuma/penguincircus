{% extends 'base.html.twig' %}

{% block title %}Keywords{% endblock %}

{% block body %}
    <h1>Keywords</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ keyword.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ keyword.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ keyword.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_keywords_index') }}">retour à la liste</a>

    <a href="{{ path('admin_keywords_edit', {'id': keyword.id}) }}"><i class="far fa-edit"></a>

    {{ include('keywords/_delete_form.html.twig') }}
{% endblock %}
