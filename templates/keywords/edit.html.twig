{% extends 'base.html.twig' %}

{% block title %}Editer le mot-clé {{ keyword.name }}{% endblock %}

{% block body %}
    <h1>Editer le mot-clé {{ keyword.name }}</h1>

    {{ include('keywords/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('keywords_index') }}">retour à la liste</a>

    {{ include('keywords/_delete_form.html.twig') }}
{% endblock %}
